<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/explore_tuban_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/explore_tuban_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateActivity"></activity>
        <activity android:name=".AddDataActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".WisataActivity" />
        <activity android:name=".PendidikanActivity" />
        <activity android:name=".MakananActivity" />
        <activity android:name=".PenginapanActivity" />
        <activity android:name=".OlehOlehActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
    </application>

</manifest>